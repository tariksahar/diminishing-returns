# Development-only dependencies. requirements.txt stays the minimum needed to
# run the analysis; a test runner does not belong in it.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest

-r requirements.txt

pytest~=9.0
# Reads the committed technical_report.pdf, so a stale compile fails a test.
pypdf~=6.14
# Drives the show-lookup app headless in tests/test_app.py.
streamlit~=1.63
