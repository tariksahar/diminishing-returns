.venv/
data/raw/
__pycache__/
*.pyc
.ipynb_checkpoints/
.pytest_cache/

# Local Jekyll build of the GitHub Pages site. The site is built by GitHub from
# the committed sources; nothing under _site/ should ever be committed.
_site/
.jekyll-cache/
.jekyll-metadata
Gemfile.lock

# Local SQLite copy of the raw IMDb dumps, rebuilt by sql/load_raw.py. Derived
# from data/raw/ (itself ignored), so it is never committed either.
data/imdb.sqlite
